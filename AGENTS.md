# AGENTS.md

## traceforge 的项目定位

traceforge 是一个 **agent trajectory 审查、标注、分析与 LLM judge 调试的研究工作台**，而不是通用标注后台系统。

本项目的核心目标是帮助算法研究人员：
- 理解轨迹结构与关键决策过程；
- 定位模型失败模式并形成可验证研究假设；
- 为后续训练数据构造、合成策略、评测设计与 verifier 机制提供闭环支撑。

## 全局工作原则

1. **先规划再实现**
   - 任何代码实现前，先在 `docs/` 中补齐需求、抽象模型与范围边界。
   - 当需求变化时，优先更新文档，再调整实现。

2. **小步提交，避免大重构**
   - 每次提交应聚焦单一目标（例如：完善一个文档章节、引入一个数据结构、添加一个视图能力）。
   - 避免“跨层大改”与一次性重写。

3. **优先保证类型清晰、数据模型稳定**
   - 领域对象与 annotation schema 必须先稳定，再扩展 UI/交互。
   - 所有功能必须围绕统一抽象模型演进，避免临时字段与隐式语义。

4. **前期使用 mock data**
   - 在数据库、权限、在线服务接入前，优先以可复现实验样例（JSON）驱动开发。
   - mock data 应覆盖典型成功/失败轨迹，便于研究与调试。

5. **实现必须遵守 docs 抽象模型**
   - 任何实现都必须与 `docs/annotation-model.md`、`docs/mvp.md`、`docs/ui-plan.md` 保持一致。
   - 若实现需求超出文档定义，先更新文档并明确变更理由，再进行编码。

## 文档优先级

在项目早期阶段，以下文档是实现依据：
- `docs/product.md`: 研究用户与产品边界
- `docs/annotation-model.md`: 统一标注抽象
- `docs/mvp.md`: 初版范围与非目标
- `docs/ui-plan.md`: 页面与交互规划

## 当前阶段约束

- 当前阶段以“需求沉淀 + 抽象建模 + MVP 设计”为主。
- 未经明确规划，不进入大规模业务代码开发。
- 评审标准优先看“问题定义是否清晰、模型是否自洽、边界是否可执行”。
